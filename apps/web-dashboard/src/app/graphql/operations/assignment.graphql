query Assignment($id: String!) {
  assignment(id: $id) {
    id
    title
    details
    course {
      id
      name
    }
    teacher {
      id
      firstName
      fatherName
      user {
        id
      }
    }
    date
    createdAt
    updatedAt
    requireSubmission
  }
}
