query GetNewsletterView($id: String!) {
  newsletter(id: $id) {
    id
    title
    content
    published
    publishedAt
    author {
      id
      name
    }
    school {
      id
      name
    }
  }
}
